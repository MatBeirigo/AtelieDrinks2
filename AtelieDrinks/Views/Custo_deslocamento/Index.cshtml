@model IEnumerable<AtelieDrinks.Models.Custo_deslocamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QtdTipoDeslocamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorTipoDeslocamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustoTipoDeslocamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.QtdTipoDeslocamento)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.ValorTipoDeslocamento)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.CustoTipoDeslocamento)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.IdTaxaDeslocamento">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdTaxaDeslocamento">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdTaxaDeslocamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
